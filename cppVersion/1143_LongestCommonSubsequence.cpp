#include <iostream>
#include <strings.h>
#include <vector>

using namespace std;
class Solution {
public:
  int longestCommonSubsequence(string text1, string text2) {
    int re1 = 0, ce1 = 0, re2 = 0, ce2 = 0;
    vector<vector<int>> dp(text1.size(), vector<int>(text2.size()));
  }
};
int main() {
  Solution s;
  cout << "hello" << endl;
}
