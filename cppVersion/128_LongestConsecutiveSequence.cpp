#include <iostream>
#include <strings.h>
#include <unordered_map>
#include <vector>

using namespace std;
class Solution {
public:
  int longestConsecutive(vector<int> &nums) {
    unordered_map<int, int> us;
    for (auto &n : nums) {
      bool le = us.contains();
      if ()
    }
  }
};

int main() {
  Solution s;
  cout << "hello" << endl;
}

